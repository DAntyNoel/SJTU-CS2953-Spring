answers-traps

1. a0-a7寄存器传递参数。13作为第三个参数放在a2中。
2. 内联函数优化处理。`f(x)`与`g(x)`相同，`printf`的调用直接计算出了结果`12`。
3. `34:	622080e7          	jalr	1570(ra) # 652 <printf>`:可以看出`printf`位置在`0x652`。
4. ra存储函数返回位置，即下一条指令`0x38`。
5. `y`的值为当前程序中寄存器a2中的值，不固定。

